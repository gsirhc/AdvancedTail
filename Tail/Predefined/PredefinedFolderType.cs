namespace Tail.Predefined
{
    /// <summary>
    /// Defines the type of item configurations in a folder
    /// </summary>
    public enum PredefinedFolderType
    {
        Highlight,
        Filter,
        User
    }
}
